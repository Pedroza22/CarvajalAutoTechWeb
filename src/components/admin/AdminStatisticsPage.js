import React from 'react';
import { getColor } from '../../utils/constants';

const AdminStatisticsPage = () => {
  return (
    <div style={{ padding: 12, color: getColor('textMuted') }}>
      Estadísticas (en construcción)
    </div>
  );
};

export default AdminStatisticsPage;


