import React from 'react';
import { getColor } from '../../utils/constants';

const AdminStudentsPage = () => {
  return (
    <div style={{ padding: 12, color: getColor('textMuted') }}>
      Estudiantes (en construcción)
    </div>
  );
};

export default AdminStudentsPage;


